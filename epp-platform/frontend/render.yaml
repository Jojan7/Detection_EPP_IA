services:
  - type: web
    name: epp-frontend
    env: node
    plan: free
    rootDir: frontend
    buildCommand: npm install && NODE_OPTIONS="--max-old-space-size=512" npm run build
    startCommand: npm start